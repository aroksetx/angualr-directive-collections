import { TimeDotsDirective } from './time-dots.directive';

describe('TimeDotsDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeDotsDirective();
    expect(directive).toBeTruthy();
  });
});
